_meta:
  source: SKILL.md
  format_version: '1.0'
frontmatter:
  name: weather
  description: Get current weather and forecasts (no API key required).
  homepage: https://wttr.in/:help
  metadata:
    nanobot:
      emoji: 🌤️
      requires:
        bins:
        - curl
title: Weather
sections:
- title: Weather
  content: Two free services, no API keys needed.
  subsections:
  - title: wttr.in (primary)
    content: 'Quick one-liner:

      ```bash

      curl -s "wttr.in/London?format=3"'
- title: 'Output: London: ⛅️ +8°C'
  content: 'bash

    curl -s "wttr.in/London?format=%l:+%c+%t+%h+%w"'
  code_blocks:
  - language: ''
    content: 'Compact format:'
- title: 'Output: London: ⛅️ +8°C 71% ↙5km/h'
  content: 'bash

    curl -s "wttr.in/London?T"

    ```


    Format codes: `%c` condition · `%t` temp · `%h` humidity · `%w` wind · `%l` location
    · `%m` moon


    Tips:

    - URL-encode spaces: `wttr.in/New+York`

    - Airport codes: `wttr.in/JFK`

    - Units: `?m` (metric) `?u` (USCS)

    - Today only: `?1` · Current only: `?0`

    - PNG: `curl -s "wttr.in/Berlin.png" -o /tmp/weather.png`'
  code_blocks:
  - language: ''
    content: 'Full forecast:'
  subsections:
  - title: Open-Meteo (fallback, JSON)
    content: 'Free, no key, good for programmatic use:



      Find coordinates for a city, then query. Returns JSON with temp, windspeed,
      weathercode.


      Docs: https://open-meteo.com/en/docs'
    code_blocks:
    - language: bash
      content: curl -s "https://api.open-meteo.com/v1/forecast?latitude=51.5&longitude=-0.12&current_weather=true"
